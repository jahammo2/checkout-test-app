{
  "addons": [
    "heroku-postgresql"
  ],
  "env": {},
  "environments": {
    "test": {
      "scripts": {
        "postdeploy": ["cp config/database.example.yml config/database.yml", "cp config/secrets.example.yml config/secrets.yml", "bin/setup"]
      }
    }
  },
  "name": "checkout-test-app",
  "scripts": {
    "postdeploy": "bin/setup"
  },
  "stack": "heroku-18"
}
